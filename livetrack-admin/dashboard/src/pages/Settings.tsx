import { useAuth } from "../store/auth";
export function Settings(){const u=useAuth(s=>s.user);return <div className="page"><div className="page-title"><div><span className="eyebrow">PREFERENCES</span><h1>Settings</h1><p>Profile, map, and display defaults.</p></div></div><div className="settings-grid"><section><h2>Profile</h2><label>Full name<input value={u?.fullName} readOnly/></label><label>Email<input value={u?.email} readOnly/></label><button className="primary small">Save changes</button></section><section><h2>Map & status</h2><label>Online timeout<select><option>60 seconds</option></select></label><label>Idle timeout<select><option>5 minutes</option></select></label><label>Date format<select><option>Local date and time</option></select></label></section></div></div>}
