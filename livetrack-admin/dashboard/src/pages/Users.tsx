import { useQuery } from "@tanstack/react-query";import { api } from "../lib/api";import type { User } from "../types";
export function Users(){const {data=[]}=useQuery({queryKey:["users"],queryFn:()=>api.get("/users").then(r=>r.data.users)});return <div className="page"><div className="page-title"><div><span className="eyebrow">ACCESS CONTROL</span><h1>Users</h1><p>Roles, access, and assigned devices.</p></div><button className="primary small">Create user</button></div><section className="table-card"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th><th>Devices</th></tr></thead><tbody>{data.map((u:User&{_count:{devices:number}})=><tr><td><b>{u.fullName}</b></td><td>{u.email}</td><td>{u.role}</td><td>{u.isActive?"Active":"Disabled"}</td><td>{u._count.devices}</td></tr>)}</tbody></table></section></div>}
