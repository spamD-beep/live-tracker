import { useQuery } from "@tanstack/react-query";import { Link } from "react-router-dom";import { Search, SlidersHorizontal } from "lucide-react";import { useState } from "react";import { api } from "../lib/api";import type { Device } from "../types";import { StatusBadge } from "../components/StatusBadge";
export function Devices(){const [q,setQ]=useState(""),[status,setStatus]=useState("ALL");const {data=[],isLoading}=useQuery({queryKey:["devices"],queryFn:()=>api.get("/devices").then(r=>r.data.devices as Device[])});const rows=data.filter(d=>(status==="ALL"||d.status===status)&&`${d.deviceName} ${d.user.fullName}`.toLowerCase().includes(q.toLowerCase()));return <div className="page"><div className="page-title"><div><span className="eyebrow">FLEET MANAGEMENT</span><h1>Devices</h1><p>Search, monitor, and manage authorized devices.</p></div></div><section className="table-card"><div className="toolbar"><label><Search/><input placeholder="Search device or owner…" value={q} onChange={e=>setQ(e.target.value)}/></label><div><SlidersHorizontal/>{["ALL","ONLINE","IDLE","OFFLINE"].map(s=><button className={status===s?"active":""} onClick={()=>setStatus(s)}>{s}</button>)}</div></div><table><thead><tr><th>Device</th><th>Owner</th><th>Platform</th><th>Tracking</th><th>Status</th><th>Battery</th><th>Last seen</th><th/></tr></thead><tbody>{isLoading?<tr><td colSpan={8}>Loading fleet…</td></tr>:rows.map(d=><tr key={d.id}><td><b>{d.deviceName}</b><small>{d.deviceUuid}</small></td><td>{d.user.fullName}</td><td>{d.platform}</td><td><span className={d.isTracking?"tracking on":"tracking"}>{d.isTracking?"Enabled":"Paused"}</span></td><td><StatusBadge status={d.status}/></td><td>{d.latestLocation?.batteryLevel??"—"}%</td><td>{d.lastSeenAt?new Date(d.lastSeenAt).toLocaleString():"Never"}</td><td><Link to={`/devices/${d.id}`}>View →</Link></td></tr>)}</tbody></table>{!isLoading&&!rows.length&&<div className="empty">No devices match these filters.</div>}</section></div>}
