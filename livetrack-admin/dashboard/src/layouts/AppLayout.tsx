import { NavLink, Outlet, useNavigate } from "react-router-dom";
import { Activity, BarChart3, Bell, Building2, ChevronLeft, LayoutDashboard, Map, Moon, Radio, Settings, Smartphone, Sun, Users } from "lucide-react";
import { useState } from "react";
import { useAuth } from "../store/auth";
export function AppLayout(){const [collapsed,setCollapsed]=useState(false),[dark,setDark]=useState(false);const {user,logout}=useAuth();const nav=useNavigate();const items=[["/","Command center",LayoutDashboard],["/map","Live map",Map],["/devices",user?.role==="MANAGER"?"Team search":"Devices",Smartphone],["/users","Users",Users],["/activity","Activity",Activity],["/quality","Quality",BarChart3],["/occupancy","Occupancy",Building2],["/settings","Settings",Settings]] as const;return <div className={dark?"app dark":"app"}><aside className={collapsed?"collapsed":""}><div className="brand"><span><Radio/></span>{!collapsed&&<b>LiveTrack<small>Admin</small></b>}</div><nav>{items.filter(x=>(x[0]!=="/users"||user?.role==="ADMIN")&&(x[0]!=="/quality"||user?.role==="ADMIN"||user?.role==="VIEWER")&&(x[0]!=="/settings"||user?.role==="ADMIN")).map(([to,label,Icon])=><NavLink key={to} to={to} end={to==="/"}><Icon/>{!collapsed&&label}</NavLink>)}</nav><button className="collapse" onClick={()=>setCollapsed(!collapsed)}><ChevronLeft/></button><div className="consent"><b>Consent-first tracking</b><span>Visible data is limited by role and active user consent.</span></div></aside><main><header><div className="connection"><i/> Live connection</div><div className="header-actions"><button onClick={()=>setDark(!dark)}>{dark?<Sun/>:<Moon/>}</button><button><Bell/></button><div className="avatar">{user?.fullName?.[0]}</div><div><b>{user?.fullName}</b><small>{user?.role.toLowerCase()}</small></div><button className="logout" onClick={()=>{logout();nav("/login")}}>Sign out</button></div></header><Outlet/></main></div>}
