{
  "name": "livetrack-admin",
  "private": true,
  "workspaces": ["backend", "dashboard"],
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,green \"npm run dev -w backend\" \"npm run dev -w dashboard\"",
    "build": "npm run build -w backend && npm run build -w dashboard",
    "test": "npm run test -w backend",
    "lint": "npm run lint -w backend && npm run lint -w dashboard"
  },
  "devDependencies": { "concurrently": "^9.1.2" }
}
